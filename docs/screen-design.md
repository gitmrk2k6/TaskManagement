# 画面設計書

関連: [要件定義書](requirements.md) / [機能要件書](functional-requirements.md)

## 1. 画面一覧

| 画面ID | 画面名 | 概要 | 関連機能 | 実装状況 |
| --- | --- | --- | --- | --- |
| SC-01 | ログイン画面 | メール・パスワードでログイン | F-02 | ❌ 未実装 |
| SC-02 | 新規登録画面 | ユーザー登録フォーム | F-01 | ❌ 未実装 |
| SC-03 | ボード一覧画面 | 自分のボードを一覧表示、新規作成 | F-03, F-04, F-05 | ❌ 未実装 |
| SC-04 | ボード詳細画面 | リストとカードを並べて操作 | F-06 〜 F-10 | ⚠️ 部分実装（タスク管理画面として実装済み。認証・ボード選択なし、列は固定3種） |

---

## 2. 画面遷移図

```mermaid
flowchart LR
    SC01[SC-01<br/>ログイン画面]
    SC02[SC-02<br/>新規登録画面]
    SC03[SC-03<br/>ボード一覧画面]
    SC04[SC-04<br/>ボード詳細画面]

    SC01 -->|新規登録リンク| SC02
    SC02 -->|登録完了| SC01
    SC01 -->|ログイン成功| SC03
    SC03 -->|ボード選択| SC04
    SC04 -->|戻る| SC03
    SC03 -->|ログアウト| SC01
    SC04 -->|ログアウト| SC01
```

---

## 3. ワイヤーフレーム（簡易）

### SC-01 ログイン画面

```text
┌────────────────────────────────┐
│         Task Management        │
├────────────────────────────────┤
│  Email    : [____________]     │
│  Password : [____________]     │
│            [  ログイン  ]      │
│                                │
│   新規登録はこちら →           │
└────────────────────────────────┘
```

### SC-02 新規登録画面

```text
┌────────────────────────────────┐
│         新規ユーザー登録       │
├────────────────────────────────┤
│  表示名   : [____________]     │
│  Email    : [____________]     │
│  Password : [____________]     │
│            [   登録   ]        │
│                                │
│   ログイン画面へ戻る →         │
└────────────────────────────────┘
```

### SC-03 ボード一覧画面

```text
┌──────────────────────────────────────┐
│ Task Management   [ + 新規ボード ]   │
├──────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐          │
│  │Board1│ │Board2│ │Board3│          │
│  └──────┘ └──────┘ └──────┘          │
└──────────────────────────────────────┘
```

### SC-04 タスク管理画面（現在の実装）

```text
┌──────────────────────────────────────────────────────────────┐
│ タスクボード  [ステータス▼] [優先度▼]  [ ＋ 新規タスク ]    │
├──────────────────────────────────────────────────────────────┤
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│ │   未着手     │ │   進行中     │ │    完了      │          │
│ ├──────────────┤ ├──────────────┤ ├──────────────┤          │
│ │[高] Card A   │ │ Card C       │ │ Card D       │          │
│ │    📅 4/30   │ │              │ │              │          │
│ │    Card B    │ │              │ │              │          │
│ └──────────────┘ └──────────────┘ └──────────────┘          │
└──────────────────────────────────────────────────────────────┘
```

> **設計との差異**: ① 認証・ボード選択なし（シングルページ直接表示）  
> ② 列は固定3種（未着手/進行中/完了）。「+ リスト」によるユーザー定義列は未実装  
> ③ ラベル色帯は未実装  
> ④ フィルターバー（ステータス・優先度）を追加実装

---

## 4. 画面要件

### SC-01 要件（ログイン画面）

| 項目 | 要件 |
| --- | --- |
| 表示項目 | タイトル、メール入力、パスワード入力、ログインボタン、新規登録リンク |
| 入力バリデーション | 必須入力チェック／メール形式チェック |
| エラー表示 | 認証失敗時にフォーム下部にメッセージ表示 |
| 遷移 | 成功→SC-03／新規登録リンク→SC-02 |

### SC-02 要件（新規登録画面）

| 項目 | 要件 |
| --- | --- |
| 表示項目 | 表示名、メール、パスワード、登録ボタン、戻るリンク |
| 入力バリデーション | メール形式・一意性／パスワード8文字以上 |
| エラー表示 | 項目ごとにエラーメッセージを表示 |
| 遷移 | 成功→SC-01／戻る→SC-01 |

### SC-03 要件（ボード一覧画面）

| 項目 | 要件 |
| --- | --- |
| 表示項目 | ボードカード一覧、新規ボード作成ボタン、ログアウトボタン |
| 表示対象 | ログインユーザーが所有するボードのみ |
| 操作 | ボード選択→SC-04／削除・名称変更／新規作成 |
| ソート | 作成日時の降順（新しいものが上） |

### SC-04 要件（タスク管理画面）

| 項目 | 現在の実装 | 将来の設計 |
| --- | --- | --- |
| 表示項目 | 固定3列（未着手/進行中/完了）、フィルターバー、新規タスクボタン | リスト群（横並び）、リスト追加ボタン |
| カード表示 | タイトル・期限バッジ（期限切れ/当日/間近で色分け）・優先度バッジ | + ラベル色帯 |
| フィルター | ステータス・優先度でフィルタリング | 全文検索・ラベルフィルタも追加 |
| 操作 | カードの作成・編集・削除／ドラッグ&ドロップでリスト内・リスト間の移動 | + リストの並び替え |
| カード詳細 | クリックでモーダル表示。優先度・ステータス・期限・説明を編集可能 | + ラベル選択 |
| レイアウト | 3列固定レイアウト | 横スクロール可。ユーザー定義リスト数に対応 |
| リアルタイム性 | 自分の操作のみ即時反映 | 同左 |
