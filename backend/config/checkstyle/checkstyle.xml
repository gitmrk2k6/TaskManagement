<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
  <property name="charset" value="UTF-8"/>
  <property name="severity" value="error"/>

  <!-- 1行 150 文字以内 -->
  <module name="LineLength">
    <property name="max" value="150"/>
    <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
  </module>

  <!-- ファイル末尾に改行 -->
  <module name="NewlineAtEndOfFile"/>

  <module name="TreeWalker">

    <!-- if / for / while / do は必ず {} で囲む -->
    <module name="NeedBraces"/>

    <!-- 1メソッド 80 行以内 -->
    <module name="MethodLength">
      <property name="max" value="80"/>
    </module>

    <!-- キーワード後にスペース (if(x) → if (x)) -->
    <module name="WhitespaceAfter">
      <property name="tokens" value="COMMA,SEMI,TYPECAST,LITERAL_IF,LITERAL_ELSE,LITERAL_WHILE,LITERAL_DO,LITERAL_FOR,LITERAL_RETURN"/>
    </module>

    <!-- セミコロンの前に余分なスペースを禁止（DOT はメソッドチェーンで使うため除外） -->
    <module name="NoWhitespaceBefore">
      <property name="tokens" value="SEMI,POST_INC,POST_DEC"/>
    </module>

    <!-- 不要な import を禁止 -->
    <module name="UnusedImports"/>

    <!-- import の重複を禁止 -->
    <module name="RedundantImport"/>

    <!-- ローカル変数のシャドウイングを検出 -->
    <module name="HiddenField">
      <property name="ignoreConstructorParameter" value="true"/>
      <property name="ignoreSetter" value="true"/>
    </module>

    <!-- 空の catch ブロックを禁止 -->
    <module name="EmptyCatchBlock"/>

    <!-- 修飾子の順序 (public static final など) -->
    <module name="ModifierOrder"/>

  </module>
</module>
